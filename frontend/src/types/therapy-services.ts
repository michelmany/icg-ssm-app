export enum ServiceType {
    SPEECH = "SPEECH",
    OCCUPATIONAL = "OCCUPATIONAL",
    PHYSICAL = "PHYSICAL",
}

export enum DeliveryMode {
    IN_PERSON = "IN_PERSON",
    VIRTUAL = "VIRTUAL"
}

export enum Status {
    SCHEDULED = "SCHEDULED",
    COMPLETED = "COMPLETED",
    MISSED = "MISSED"
}